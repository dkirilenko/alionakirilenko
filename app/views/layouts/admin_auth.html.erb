<!DOCTYPE html>
<html>
<head>
  <title>Admin Login</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

  <style>
    /* If no Tailwind, this keeps it neat */
    body {
      background: #f3f4f6;
      font-family: system-ui, sans-serif;
    }
    .card {
      width: 560px;
      margin: 100px auto;
      padding: 32px;
      background: white;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.1);
    }
    input {
      width: 100%;
      padding: 10px;
      margin-top: 8px;
      margin-bottom: 16px;
      border-radius: 6px;
      border: 1px solid #ddd;
    }
    button {
      width: 100%;
      background: #4f46e5;
      color: white;
      padding: 10px;
      border-radius: 6px;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background: #4338ca;
    }
  </style>
</head>
<body>
  <div class="card">
    <%= yield %>
  </div>
</body>
</html>
